{
  "name": "the-last-hair-realtime",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "vitest run",
    "build": "node stamp.mjs && npm test && wrangler deploy --dry-run --outdir .build",
    "deploy": "node stamp.mjs && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.1",
    "vitest": "3.2.4"
  }
}